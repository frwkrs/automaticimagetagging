The names of the different folders are the labels (in this case labels are the tags that describe what's on the picture)
for the images that are in the folder. An image can be in different folders since it can have different labels